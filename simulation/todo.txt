todos:

* Comment!
	* graph
	* nodes
	* route table
	* aux function
	* include mthods in class description?
* function to translate back from INternet to Networkx
* router table for victim
* with more than 1 --> osciallations --> still there?
* graph generation also creates:
	- attack magnitude limit [a, b]
	- ally capabilities
* reproduciability via seed to svae and repeat problematic examples
* adaptive attack volume
* attack volume gaussian
* different attack modes